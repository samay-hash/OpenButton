{
  "name": "openbutton-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --recursive run dev",
    "build": "pnpm --parallel --recursive run build"
  }
}
